# Virtitta

Virtitta is a compact internal web interface for browsing `virpipa` run outputs, assigning QC status,
adding comments, and launching IGV from imported per-sample QC summaries.

Configuration lives in `virtitta.toml`. Start from `virtitta.example.toml`.

